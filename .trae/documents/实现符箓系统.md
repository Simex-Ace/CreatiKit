# 实现符箓系统

## 1. 系统概述
符箓系统将为修真游戏添加新的玩法维度，玩家可以制作和使用各种符箓，用于战斗、修炼、探险等多个方面。符箓将具有不同的效果和持续时间，为游戏增加策略性和多样性。

## 2. 实现步骤

### 2.1 添加类型定义
- 在`types.ts`中添加符箓相关的接口和类型
- 定义符箓类型、效果、制作材料等

### 2.2 创建符箓数据
- 在`data`目录下创建`talismans.ts`文件
- 定义各种符箓的属性、效果、制作材料等

### 2.3 实现符箓制作系统
- 在`game-core.ts`中添加符箓制作相关的方法
- 实现符箓制作的逻辑，包括材料消耗、成功率、冷却时间等

### 2.4 实现符箓使用系统
- 在`game-core.ts`中添加符箓使用相关的方法
- 实现符箓的效果应用、持续时间、冷却时间等

### 2.5 添加UI组件
- 创建`TalismanPanel.tsx`组件，用于显示和管理符箓
- 添加符箓制作界面和符箓背包

### 2.6 集成到游戏中
- 在主页面中添加符箓标签页
- 添加符箓相关的任务和成就

## 3. 具体实现细节

### 3.1 类型定义
```typescript
// 符箓类型
export type TalismanType = 'attack' | 'defense' | 'cultivation' | 'exploration' | 'utility';

// 符箓接口
export interface Talisman {
  id: string;
  name: string;
  description: string;
  type: TalismanType;
  effect: {
    attackBoost?: number;
    defenseBoost?: number;
    cultivationSpeedBoost?: number;
    qiGatherRateBoost?: number;
    explorationSuccessRateBoost?: number;
    damageReduction?: number;
    [key: string]: any;
  };
  duration: number; // 效果持续时间（秒）
  cooldown: number; // 冷却时间（秒）
  rarity: 'common' | 'rare' | 'epic' | 'legendary';
  value: number;
}

// 符箓制作配方接口
export interface TalismanRecipe {
  id: string;
  name: string;
  description: string;
  talismanId: string;
  ingredients: {
    id: string;
    quantity: number;
  }[];
  requiredLevel: CultivationLevel;
  baseSuccessRate: number;
  expGain: number;
  duration: number; // 制作时间（秒）
}

// 符箓状态接口
export interface TalismanState {
  recipes: TalismanRecipe[]; // 已解锁的符箓配方
  currentTalisman?: string; // 当前正在制作的符箓ID
  progress: number; // 制作进度 (0-100)
  isCrafting: boolean; // 是否正在制作符箓
  startTime?: number; // 开始制作的时间
  lastCraftTime: number; // 上次制作时间
  successCount: number; // 成功制作次数
  failedCount: number; // 失败制作次数
  skillLevel: number; // 符箓制作技能等级
  skillExp: number; // 符箓制作技能经验
  maxSkillExp: number; // 升级所需经验
  activeTalismans: Array<{
    talisman: Talisman;
    startTime: number;
  }>; // 当前激活的符箓效果
  knownTalismans: string[]; // 已知的符箓ID列表
  maxConcurrentTalismans: number; // 最大同时使用的符箓数量
}
```

### 3.2 符箓数据示例
```typescript
export const talismans: Talisman[] = [
  {
    id: 'talisman_attack',
    name: '攻击符箓',
    description: '临时提升攻击力',
    type: 'attack',
    effect: {
      attackBoost: 0.2
    },
    duration: 300,
    cooldown: 600,
    rarity: 'common',
    value: 50
  },
  // 更多符箓...
];

export const talismanRecipes: TalismanRecipe[] = [
  {
    id: 'recipe_attack_talisman',
    name: '攻击符箓配方',
    description: '制作攻击符箓的配方',
    talismanId: 'talisman_attack',
    ingredients: [
      { id: 'spiritGrass', quantity: 5 },
      { id: 'materials', quantity: 10 }
    ],
    requiredLevel: 'qi_refining_1',
    baseSuccessRate: 0.8,
    expGain: 20,
    duration: 10
  },
  // 更多配方...
];
```

### 3.3 核心功能实现
- `startCraftTalisman(recipeId: string)`: 开始制作符箓
- `updateTalismanCrafting()`: 更新符箓制作进度
- `completeTalismanCrafting()`: 完成符箓制作
- `useTalisman(talismanId: string)`: 使用符箓
- `updateActiveTalismans()`: 更新激活的符箓效果

### 3.4 UI组件
- 符箓制作界面：显示可用配方、材料消耗、成功率等
- 符箓背包：显示已拥有的符箓、使用按钮等
- 激活符箓列表：显示当前激活的符箓、剩余时间等

## 4. 预期效果
- 为游戏增加新的玩法和策略
- 丰富玩家的游戏体验
- 增加游戏的可玩性和深度
- 与现有系统良好集成

## 5. 后续扩展
- 添加更多类型的符箓和效果
- 实现符箓升级系统
- 添加符箓套装效果
- 实现符箓的传承和学习系统

这个计划将为修真游戏添加一个完整的符箓系统，为玩家提供新的玩法和策略选择。