# 修复 OTP 过期错误

## 问题描述

点击密码重置邮件中的链接后，显示错误：
```json
{"code":403,"error_code":"otp_expired","msg":"Email link is invalid or has expired"}
```

## 问题原因

1. **链接过期**：Supabase 的密码重置链接通常有效期为 **1 小时**
2. **链接被多次使用**：每个链接只能使用一次
3. **URL 配置问题**：回调 URL 配置不正确导致链接无法正确验证

## 已完成的修复

### 1. 改进回调路由错误处理
- ✅ 正确捕获 OTP 过期错误
- ✅ 将错误信息传递给重置密码页面
- ✅ 显示友好的错误提示

### 2. 改进重置密码页面
- ✅ 显示来自 URL 参数的错误信息
- ✅ 区分"链接过期"和"链接无效"
- ✅ 提供清晰的提示和操作指引

## 解决方案

### 方案 1：立即重新申请（推荐）

1. 返回首页
2. 点击"忘记密码"
3. 重新输入邮箱
4. **立即**点击邮件中的链接（不要等待太久）

### 方案 2：检查 Supabase 配置

确保以下配置正确：

1. **URL Configuration**：
   - 网站网址：`https://creatikit.asia`（单个 URL）
   - 重定向 URL 包含：
     ```
     https://creatikit.asia/auth/callback
     https://creatikit.asia/auth/reset-password
     ```

2. **邮件模板**：
   - 进入 **Authentication** > **Email Templates**
   - 检查 **Reset Password** 模板
   - 确保链接格式正确

### 方案 3：延长 OTP 有效期（如果可能）

Supabase 默认 OTP 有效期为 1 小时，这是安全设置，不建议修改。

## 最佳实践

1. **及时使用链接**：
   - 收到邮件后立即点击链接
   - 不要等待超过 1 小时

2. **检查邮件时间**：
   - 如果邮件是几小时前发送的，链接可能已过期
   - 需要重新申请

3. **使用桌面浏览器**：
   - 某些手机浏览器可能无法正确处理重定向
   - 建议在桌面浏览器中打开链接

4. **检查网络**：
   - 确保网络连接正常
   - 某些网络环境可能阻止重定向

## 验证修复

修复后，当链接过期时：
1. 会显示友好的错误页面（而不是 JSON 错误）
2. 提示"链接已过期，请重新申请密码重置"
3. 提供返回首页的按钮
4. 显示链接有效期提示

## 如果问题仍然存在

1. **检查 Supabase 日志**：
   - 进入 **Logs** > **Auth Logs**
   - 查看详细的错误信息

2. **尝试不同的设备**：
   - 在桌面浏览器中测试
   - 排除手机浏览器的问题

3. **检查邮件内容**：
   - 查看邮件中的链接是否完整
   - 确保链接没有被截断

4. **联系 Supabase 支持**：
   - 如果配置正确但问题仍然存在
   - 可能是 Supabase 服务端的问题





